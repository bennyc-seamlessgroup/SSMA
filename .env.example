NEXT_PUBLIC_APP_NAME=Currenc Intelligence
DEFAULT_TIMEZONE=America/New_York
NEXT_PUBLIC_API_CACHE_SECONDS=900
NEXT_PUBLIC_DATA_STATUS_POLL_SECONDS=300
NEXT_PUBLIC_COGNITO_DOMAIN=auth.currenc.capital
NEXT_PUBLIC_COGNITO_CLIENT_ID=69l2ucuajiebaj4cau1hvq231f
NEXT_PUBLIC_REDIRECT_URI=http://localhost:3000/callback
# Local Cognito callbacks are registered for localhost. Opening development
# through a private LAN IP automatically returns the login page to localhost
# before OAuth begins so PKCE state remains on the callback origin.
NEXT_PUBLIC_API_GATEWAY_URL=https://3flfpju5k8.execute-api.us-east-1.amazonaws.com/dev
NEXT_PUBLIC_WS_API_URL=wss://xd5onp9o52.execute-api.us-east-1.amazonaws.com/dev
# Server-only native Cognito demo authentication. The app client must allow USER_PASSWORD_AUTH and have no client secret.
DEMO_COGNITO_CLIENT_ID=
DEMO_ACCOUNT_PASSWORD=
