// Production note: replace the static SVG fallback with ECharts instances if desired.
// The included static-demo.html is already pre-rendered for reliable Playwright/Puppeteer PDF export.
window.__REPORT_READY__ = true;
