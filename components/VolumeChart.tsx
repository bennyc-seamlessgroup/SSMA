export function VolumeChart({ points }: { points: number[] }) { const width = 700; const height = 220; const max = Math.max(...points); const barWidth = width / points.length; return <div className="chart panel"><svg viewBox={`0 0 ${width} ${height}`} preserveAspectRatio="none">{points.map((p, i) => { const barH = (p / (max || 1)) * (height - 24); return <rect key={i} x={i*barWidth + 8} y={height - barH - 10} width={Math.max(barWidth - 16, 8)} height={barH} rx="8" fill="#34d399" opacity="0.85" />; })}</svg></div>; }
