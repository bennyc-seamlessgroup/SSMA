export function PriceChart({ points }: { points: number[] }) { const width = 700; const height = 220; const min = Math.min(...points); const max = Math.max(...points); const xStep = width / Math.max(points.length - 1, 1); const scaleY = (v:number) => height - ((v - min) / (max - min || 1)) * (height - 20) - 10; const path = points.map((p, i) => `${i===0?'M':'L'} ${i*xStep} ${scaleY(p)}`).join(' '); return <div className="chart panel"><svg viewBox={`0 0 ${width} ${height}`} preserveAspectRatio="none"><defs><linearGradient id="priceFill" x1="0" x2="0" y1="0" y2="1"><stop offset="0%" stopColor="#82aaff" stopOpacity="0.35"/><stop offset="100%" stopColor="#82aaff" stopOpacity="0.03"/></linearGradient></defs><path d={`${path} L ${width} ${height} L 0 ${height} Z`} fill="url(#priceFill)" /><path d={path} fill="none" stroke="#82aaff" strokeWidth="3" strokeLinejoin="round" strokeLinecap="round" />{points.map((p, i) => <circle key={i} cx={i*xStep} cy={scaleY(p)} r="3.5" fill="#dbeafe" />)}</svg></div>; }
